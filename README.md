Translation System UI

نظام إدارة ترجمة متكامل مبني باستخدام Next.js 14 و Tailwind CSS. يوفر واجهة مستخدم متعددة الأدوار لإدارة عمليات الترجمة.

https://img.shields.io/badge/Next.js-14.0-black?style=flat&logo=next.js
https://img.shields.io/badge/React-19.2-blue?style=flat&logo=react
https://img.shields.io/badge/Tailwind_CSS-4.1-38B2AC?style=flat&logo=tailwind-css
https://img.shields.io/badge/TypeScript-5.0-3178C6?style=flat&logo=typescript

✨ المميزات

· واجهة متعددة الأدوار: مدير، مشرف، مدقق، مترجم، مدخل بيانات
· تصميم حديث: واجهة مستخدم متجاوبة مع Tailwind CSS
· أداء عالي: مبنية باستخدام Next.js 14 مع App Router
· سهولة الاستخدام: واجهة بديهية وسهلة التنقل

📋 متطلبات التشغيل

· Node.js: الإصدار 18.17 أو أحدث
· pnpm: الإصدار 8 أو أحدث (مفضل) أو npm
· Backend API: يجب تشغيل API على http://localhost:5296

🚀 البدء السريع

1. تثبيت التبعيات

```bash
# استخدام pnpm (مفضل)
pnpm install

# أو باستخدام npm
npm install
```

2. تشغيل خادم التطوير

```bash
# باستخدام pnpm
pnpm run dev

# أو باستخدام npm
npm run dev
```

3. الوصول للتطبيق

افتح المتصفح على الرابط: http://localhost:3000

⚙️ الإعداد

إعداد المتغيرات البيئية

المشروع مهيأ افتراضياً للاتصال بـ http://samali1-001-site1.stempurl.com/api. لتغيير الرابط:

1. إنشاء ملف .env.local في مجلد المشروع:

```bash
cp .env.example .env.local
```

1. تعديل الملف:

```env
NEXT_PUBLIC_API_URL=http://localhost:5296/api
```

المتغيرات المتاحة

المتغير الوصف القيمة الافتراضية
NEXT_PUBLIC_API_URL رابط API الخلفي http://samali1-001-site1.stempurl.com/api
NEXT_PUBLIC_APP_NAME اسم التطبيق نظام الترجمة

📁 هيكلية المشروع

```
translation-system-ui/
├── app/                    # صفحات التطبيق (App Router)
│   ├── manager/           # لوحة تحكم المدير
│   ├── supervisor/        # لوحة تحكم المشرف
│   ├── reviewer/          # لوحة تحكم المدقق
│   ├── translator/        # لوحة تحكم المترجم
│   ├── data-entry/        # لوحة تحكم مدخل البيانات
│   ├── layout.tsx         # التخطيط الرئيسي
│   └── page.tsx           # الصفحة الرئيسية
├── components/            # مكونات قابلة لإعادة الاستخدام
│   ├── ui/               # مكونات واجهة مستخدم عامة
│   ├── forms/            # مكونات النماذج
│   └── layout/           # مكونات التخطيط
├── lib/                   # مكتبات مساعدة
│   ├── api/              # دوال API
│   ├── utils/            # دوال مساعدة
│   └── constants/        # ثوابت التطبيق
├── types/                 # تعريفات TypeScript
├── public/               # ملفات ثابتة
│   └── assets/           # الصور والأيقونات
└── styles/               # ملفات CSS العالمية
```

👥 الأدوار والمستويات

الدور الوصف الصلاحيات
المدير الإدارة الكاملة للنظام جميع الصلاحيات
المشرف إدارة الفرق والمشاريع إدارة المترجمين والمدققين
المدقق مراجعة الترجمة مراجعة وتدقيق النصوص المترجمة
المترجم تنفيذ الترجمة ترجمة النصوص الموكلة إليه
مدخل البيانات إدخال النصوص الأصلية إدخال النصوص المطلوب ترجمتها

🛠️ الأوامر المتاحة

```bash
# تشغيل خادم التطوير
pnpm run dev

# بناء المشروع للإنتاج
pnpm run build

# تشغيل المشروع بعد البناء
pnpm run start

# فحص TypeScript
pnpm run lint

# فحص ESLint
pnpm run lint:eslint
```

🔧 الأدوات والتقنيات المستخدمة

· Next.js 14: إطار عمل React مع App Router
· React 19: مكتبة واجهة المستخدم
· Tailwind CSS 4: إطار عمل CSS
· TypeScript 5: لغة البرمجة مع التنميط الثابت
· Radix UI: مكونات واجهة مستخدم قابلة للوصول
· React Hook Form: إدارة النماذج
· Lucide React: مكتبة الأيقونات

🌐 النشر

النشر على Vercel (موصى به لـ Next.js)

```bash
# تثبيت CLI
npm i -g vercel

# النشر
vercel --prod
```

النشر على خادم تقليدي

```bash
# 1. بناء المشروع
pnpm run build

# 2. نسخ مجلد .next إلى الخادم
# 3. تشغيل الخادم
pnpm run start
```

🔍 استكشاف الأخطاء وإصلاحها

المشكلة: لا يمكن الاتصال بـ API

```bash
# تحقق من تشغيل API
curl http://localhost:5296/api/health

# تحقق من المتغيرات البيئية
echo $NEXT_PUBLIC_API_URL
```

المشكلة: أخطاء في التبعيات

```bash
# تنظيف وإعادة التثبيت
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

المشكلة: مشاكل في TypeScript

```bash
# فحص الأخطاء
pnpm run lint

# إصلاح الأخطاء تلقائياً
pnpm run lint --fix
```

🤝 المساهمة

1. Fork المشروع
2. إنشاء فرع للميزة (git checkout -b feature/AmazingFeature)
3. Commit التغييرات (git commit -m 'Add some AmazingFeature')
4. Push إلى الفرع (git push origin feature/AmazingFeature)
5. فتح Pull Request

📄 الرخصة

هذا المشروع مرخص تحت رخصة MIT - انظر ملف LICENSE للمزيد من التفاصيل.

📞 الدعم

للدعم أو الإبلاغ عن مشاكل:

1. افتح Issue في GitHub
2. تواصل مع فريق التطوير

---

تم التطوير باستخدام Next.js و Tailwind CSS
